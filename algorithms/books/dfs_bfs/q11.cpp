#include <bits/stdc++.h>

#define MAX_SIZE 3

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    
    vector<int[MAX_SIZE]> visited;
    
    pair<int, int> bottles[MAX_SIZE];
    
    // set up the capacity
    bottles[0].first = bottles[0].second = 8;
    bottles[1].first = 5;
    bottles[2].first = 3;
    bottles[1].second = bottles[2].second = 0;
    
    
    
    return 0;
}