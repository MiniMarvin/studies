#ifndef __STUDY_USEFULL
#define __STUDY_USEFULL

/**
 * @brief      Generate a random size array with random numbers.
 *
 * @param      arr   The arr where it's going to be allocated.
 *
 * @return     The array size.
 */
int randArr(int *&arr);

#endif